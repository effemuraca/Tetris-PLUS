document.onload = function () {
    // richiedi la classifica da classifica.php
    let myObj;
   /* const richiesta = new XMLHttpRequest();
    richiesta.open('GET', 'classifica.php', true);
    richiesta.onload = function () {
        myObj = JSON.parse(this.responseText);
    }
    richiesta.send();

    // stampa la classifica
    const classifica = document.getElementById('body_classifica');
    console.log(myObj);*/




};
